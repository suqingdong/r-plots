```bash
Rscript barplot.R -h

Usage: barplot.R [options]

Options:
	-i INFILE, --infile=INFILE
		The input file.

	-o OUTFILE, --outfile=OUTFILE
		The output file.

	--title=TITLE
		The title of the picture.

	--xlab=XLAB
		The xlab of the picture.

	--ylab=YLAB
		The ylab of the picture.

	--width=WIDTH
		The width of the picture.

	--height=HEIGHT
		The height of the picture.

	--position=POSITION
		The position of bar["stack", "dodge" default="stack"]

	--percent
		show Y lab in percent mode[default "FALSE"].

	-h, --help
		Show this help message and exit

	Contact: suqingdong@novogene.com
```